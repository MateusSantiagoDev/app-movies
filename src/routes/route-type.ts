export enum RouterPath {
  LOGIN = "/",
  REGISTRATION = "/registration",
  HOME = "/home",
  PROFILES = "/profile",
  MOVIES = "/movie",
  SERIES = "/serie",
  ANIMES = "/animes",
  FORM = "/form",
  FORM_UPDATE = "/form-update/:id",
}
